# ServerPanel Configuration
# Copy this file to .env and modify as needed

# Server Configuration
PORT=8888
GIN_MODE=release

# Trusted Proxies
# Comma-separated list of trusted proxy IPs (e.g., when behind Nginx/Apache)
# Leave empty for direct access (no reverse proxy)
# Examples:
#   TRUSTED_PROXIES=127.0.0.1,::1
#   TRUSTED_PROXIES=10.0.0.1,172.16.0.1
TRUSTED_PROXIES=

# Security - IMPORTANT: Change these in production!
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-this-in-production

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# If not set, same-origin requests (frontend served from same server) are automatically allowed
# For external frontends, add their origins here
# Examples:
#   CORS_ORIGINS=http://localhost:3000,http://localhost:8888
#   CORS_ORIGINS=https://panel.example.com,https://admin.example.com
# Leave empty or comment out to allow only same-origin requests in production
CORS_ORIGINS=

# File Manager
# Base path for file operations (restricts file management to this directory)
# Set to / to allow access to the full filesystem (default)
FILE_MANAGER_BASE_PATH=/

# Database
# SQLite database file location (default: ./serverpanel.db)
# DB_PATH=/opt/serverpanel/serverpanel.db
